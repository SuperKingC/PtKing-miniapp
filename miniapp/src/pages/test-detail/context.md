# 工作记录

- 时间：2026-09-12（第三轮）
- 原因：用户三点要求——①介绍页按钮要与测试中心「开始测试」同色同形；②item 边缘与阴影要和外面的卡片一致；③删掉「重新开始」按钮。
- 修改：①主 CTA 改挂全局 `--action-btn-bg / --action-btn-shadow`（从 test-page__card-go 提升，逐像素采样奶杏暖陶剖面），与列表按钮共用一层；②`.test-detail__card` 补 `box-shadow: var(--shadow-card)`（此前是全包唯一不挂 item 影的卡片）；③删 `.test-detail__restart` 节点/样式与 `startPlay(restart)` 参数，重开改由答题页离开确认承担（保留 `hasDraft` 驱动「继续测试」文案）。实机验收：卡下缘出现接触影带、主按钮奶杏渐变，有草稿仅显示单一「继续测试」。

- 时间：2026-09-12（第二轮）
- 原因：用户要求介绍栏与下方按钮作为一组，在「返回钮底边 → 屏幕底部」区域内整体居中。
- 修改：根节点改纵向 flex，`padding-top = --back-top + 88rpx + 24rpx`（上界为返回钮底边），下留白同取 24rpx 与上留白等量——这样 auto 边距才把内容组居中在该区域几何中点；下留白混入 `env(safe-area-inset-bottom)` 会把整组顶高（实测偏差 ~19px，去掉后上下间隙相等）。介绍卡/免责/按钮包进 `.test-detail__body`，用上下 `margin: auto` 均分剩余空间；内容高于区域时 auto 归零、从顶部起可正常滚动不裁切。实机验收：两个测试（恋爱脑/吵架和好）上下间隙均相等（35.3px / 21.8px）。

- 时间：2026-09-12
- 原因：详情页返回钮视觉偏高，用户要求与右上胶囊「三个点」图标中心对齐（测试中/测试报告两页同诉求）。
- 修改：三页共用的 `navMetrics.resolveFixedBackTopPx` 由「胶囊底边 − 钮高」改为「胶囊底边 − 胶囊高/2 − 钮高/2」；本页无独立样式改动，实测返回钮中心 67px ≈ 胶囊中心 66.5px。

- 时间：2026-09-11 12:50
- 原因：用户反馈四点：顶距偏高（内容顶进胶囊）、没有返回键、封面 icon 要去掉、答题/报告页也不要系统「测测子」标题栏。
- 修改：根因是 Taro 把 scss 里 --page-top-inset 兜底值 88px 编译成 88rpx（44px），兜底腰斩成 49px 顶到胶囊上面；改为根节点内联 style 注入 topInsetStyle()（与 tab 页同法，实测 95px）。删封面图与四张 spot PNG（资产退役）；加左上圆形返回键（getCurrentPages>1 走 navigateBack，栈底 switchTab 回测试中心）。

- 时间：2026-09-09 17:00
- 原因：详情封面改 ui-4 软陶毡面。
- 修改：`index.tsx` 引用 `spot-*-v2.png`。

- 时间：2026-09-09 16:30
- 原因：顶部标题统一为测测子。
- 修改：`index.config.ts` 导航标题改为「测测子」。

- 时间：2026-09-09 11:20
- 原因：详情页像说明书。
- 修改：顶部加分类封面；介绍只留两段；注意改单行；免责收到按钮上方。

- 时间：2026-09-08 11:35
- 原因：去掉「你会看到」，注意改成原答题指引文案。
- 修改：`index.tsx` 删除 howto 栏，「注意」固定为「没有标准答案，按最近的通常状态和第一反应选择即可。」；`index.scss` 去掉 howto 样式。
