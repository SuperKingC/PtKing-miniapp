// 底栏「我的」选中态 v5(2026-09-12 用户第四次反馈):沿用 v4b 的感觉(爪举到肩高、掌心朝观者),
//   但必须是【猫的左手】举起来 —— v4b 举的是猫的右手(画面上出现在左侧)。
//   猫正对观者时「猫的左手」在【画面右侧】;提示词里同时写死「猫的左手」与「画面右侧」消歧。
// 其余全部沿用 v4/v3:测测子角色、奶油身、正对端正、藏尾巴、雾蓝纯色底、无地面影。
// 出 3 份(同姿态微差)供挑,因为镜像后模型可能左右手画反/变形。
// 用法:node compose-me-pose5.mjs → prompts-me-pose5.txt(配 --char char-ref.local.jpg)
import fs from 'node:fs'
import path from 'node:path'
import { fileURLToPath } from 'node:url'

const root = path.dirname(fileURLToPath(import.meta.url))

const STYLE = [
  '高调奶油软陶 3D 治愈插画(claymorphism)',
  '哑光聚合软陶, 表面干净光滑细腻带细纸纹微粒, 毡面纤维感弱, 圆润饱满, 无硬描边',
  '高调影棚光, 环境光足, 不要浓毡纤维不要暗调重影, 不要水彩勾线不要彩铅排线不要扁平矢量色块',
  '极简治愈, 高级感',
].join('. ')

const PRE = '只要一枚底部小图标, 不要界面不要底栏胶囊不要卡片不要成对'
const BG = '背景是一整片纯色平涂的柔和雾蓝灰色(色值约#9fb4c2), 平涂无渐变无纹理, 主体四周都是这个底色, 主体与背景之间干净分离'

const CHAR = [
  '严格照参考图里那只「测测子」猫的角色设计来画, 毛色与五官完全一致',
  '身体是奶油象牙白色(接近#f3ead9)',
  '只有头顶三道浅杏色条纹、两只耳朵外侧各一小块浅杏色、耳内是浅杏色, 身上其余部分都是奶油白',
  '一张大大的圆脸, 两只大而圆的深棕色眼睛, 一个小巧的浅杏色三角形鼻子, 鼻子下一道小小的微笑嘴形',
  '两颊有淡淡的杏色腮红, 脸颊两侧各有三根细胡须, 圆圆的耳朵立在头顶两侧',
].join(', ')

// 核心:猫的左手举起,掌心肉垫正对观者(猫正对观者→猫左手在画面右侧)
const SIT_LEFT_PAW = [
  '一只圆润小猫用猫的方式坐在后腿上, 正面朝向观者, 完全对称端正, 头直接坐在肩上',
  '猫的右手(画面上位于左侧的那条前腿)笔直并拢踩在地面上支撑身体',
  '猫的左手(画面上位于右侧的那条前腿)抬起, 举到大约胸口到肩膀的高度, 爪掌朝向观者',
  '抬起的是猫自己的左手, 注意左右不要画反: 抬起的爪必须出现在画面的右半边',
  '抬起的那只爪掌掌心完全正对观者, 能清楚看到掌心的粉色肉垫: 一大块粉色主肉垫, 上方四枚小小的粉色趾垫, 粉色要明显、干净、边缘圆润; 看到的是爪掌正面(肉垫), 不是爪背',
  '后腿收拢折叠在身体两侧, 大腿鼓起形成圆润饱满的臀部坐在地上, 尾巴完全藏在身后看不见',
].join(', ')

const FACE = '表情开心眯起眼睛笑, 两团圆腮红更明显'

const POST = [
  '无底板无胶囊无道具, 单个图标居中, 主体约占画面的四分之三, 主体外接框接近正方形',
  '无描边, 无文字。细腻毡面颗粒有体积',
  '严格是四足猫科动物的坐姿: 绝不要像人或泰迪熊那样坐, 不要把两只前爪交叉抱在胸前, 不要双臂抱胸',
  '绝对不要露出尾巴, 尾巴必须完全藏在身体后面看不见',
  '绝对不要任何地面投影或接触阴影, 猫脚下不要有影, 主体直接坐在纯色背景上',
].join(', ')

// 三份同姿态微差(镜像后易左右反/爪形变形,多给几张挑)
const TWEAKS = [
  { id: 'a', text: '抬起的猫左手掌心正对观者, 爪掌画得大而清楚, 四枚趾垫整齐' },
  { id: 'b', text: '抬起的猫左手稍微转向观者, 主肉垫与四枚趾垫都完整可见' },
  { id: 'c', text: '抬起的猫左手举得稍高一点到肩头, 掌心朝前, 粉色肉垫醒目' },
]

const lines = []
lines.push('# 「我的」选中态 v5(2026-09-12):沿用 v4b 感觉,但改为【猫的左手】举起(画面右侧)。')
lines.push('# 由 compose-me-pose5.mjs 生成,勿手改。只做选中态 3 份;未选中沿用 v3。本轮不接包。')
lines.push(`STYLE=${STYLE}`)
lines.push('')
for (const t of TWEAKS) {
  lines.push(`icon-tab-me-active-sit5${t.id}|${PRE}。{STYLE} ${BG}。${SIT_LEFT_PAW}, ${t.text}, ${CHAR}, ${FACE}。${POST}`)
  lines.push('')
}
fs.writeFileSync(path.join(root, 'prompts-me-pose5.txt'), lines.join('\n'), 'utf8')
const n = lines.filter((l) => l && !l.startsWith('#') && !l.startsWith('STYLE=')).length
console.log(`compose-me-pose5: 写入 ${n} 条 → prompts-me-pose5.txt`)
